<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/event/{{event.id}}">{{event_title}}</a></h4>
        <p>{{event_description}}</p>
        <p class="card-body">{{event_date}}</p>
        <button type="submit" class="btn start btn-primary mt-3 mb-5" onclick="window.location.href = '/event/{{id}}/rsvp'">Respond</button>
    </div>
        
</div>