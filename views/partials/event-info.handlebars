<div class="card">
    <div class="card-header">
       <h4><a class="link" href="/event/{{event.id}}">{{event_title}}</a></h4>
        <p>{{event.event_description}}</p>
        <p>Posted by {{user.username}} on {{format_date event_date}}</p>
        <p class="card-body">{{event_date}}</p>
    </div>
        
</div>